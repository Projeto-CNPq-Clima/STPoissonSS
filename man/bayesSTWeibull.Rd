% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesSTWeibull.R
\name{bayesSTWeibull}
\alias{bayesSTWeibull}
\title{Bayesian Estimation for Spatiotemporal Nonhomogeneous Poisson Model with Weibull Intensity and State-Space Prior}
\usage{
bayesSTWeibull(
  data,
  sites,
  tau_breaks,
  Ftw,
  Ftm,
  G,
  Q,
  prior = list(M0 = as.matrix(rep(0, nrow(Ftw[1, , ]))), C0 = diag(100, nrow(Ftw[1, ,
    ])), S0 = diag(0.001, nrow(Ftw[1, , ])), n0 = 10, M1 = as.matrix(rep(0, nrow(Ftw[1, ,
    ]))), M1 = as.matrix(rep(0, nrow(Ftm[1, , ]))), C1 = diag(100, nrow(Ftw[1, , ])), S1
    = diag(0.001, nrow(Ftw[1, , ])), S1 = diag(0.001, nrow(Ftm[1, , ])), n1 = 10, aa1 =
    2.1, bb1 = 1.1, aa2 = 2.1, bb2 = 1.1, c3 = 5.197782e-08, d3 = 2.279865e-05, c2 =
    5.197782e-08, d2 = 2.279865e-05),
  iteration,
  burnin,
  jump
)
}
\arguments{
\item{data}{A list or array of observed counts following a nonhomogeneous Poisson process structure, as used in other spatiotemporal Poisson models.}

\item{sites}{A matrix or data structure specifying the spatial coordinates or structure of observation sites, as in standard spatial Poisson models.}

\item{tau_breaks}{A numeric vector containing the internal breakpoints \eqn{(\tau_1, \ldots, \tau_{L})} that partition the time interval \eqn{(0, T]} into \eqn{L} disjoint subintervals. The vector should be sorted in increasing order and must not include the initial time 0 or the endpoint T.}

\item{Ftw}{An array of dimension \verb{L × p × n}. The l-th slice \code{Xm[l,,]} corresponds to the transposed covariate matrix \verb{X'_l} used to explain the latent vector \code{W_l}.}

\item{Ftm}{An array of dimension \verb{L × q × n}. The l-th slice \code{Zm[l,,]} corresponds to the transposed covariate matrix \verb{X'_l} used to explain the latent vector \code{M_l}.}

\item{G}{An array of dimension \verb{L × p × p}. Each slice \code{G[l,,]} corresponds to the known evolution matrix \code{G_l} of size \verb{p × p}, which governs the temporal evolution of the latent process associated with \code{W_l}.}

\item{Q}{An array of dimension \verb{L × q × q}. Each slice \code{Q[l,,]} corresponds to the known evolution matrix \code{Q_l} of size \verb{q × q}, which governs the temporal evolution of the latent process associated with \code{M_l}.}

\item{prior}{A list containing the hyperparameters for the prior distributions:
\describe{
\item{M0, C0}{Prior mean and covariance for the initial state of \code{Psi}, i.e., \code{Psi_0 ~ N(M0, C0)}}
\item{S0, n0}{Scale matrix and degrees of freedom for the inverse-Wishart prior on \code{V_Psi}, i.e., \code{V_Psi ~ Inv-Wishart(S0, n0)}}
\item{M1, C1}{Prior mean and covariance for the initial state of \code{beta}, i.e., \code{beta_0 ~ N(M1, C1)}}
\item{S1, n1}{Scale matrix and degrees of freedom for the inverse-Wishart prior on \code{V_beta}, i.e., \code{V_beta ~ Inv-Wishart(S1, n1)}}
\item{aa1, bb1}{Shape and scale parameters for the inverse-gamma prior on \verb{sigma^2_w}, i.e., \verb{sigma^2_w ~ IG(aa1, bb1)}}
\item{aa2, bb2}{Shape and scale parameters for the inverse-gamma prior on \verb{sigma^2_m}, i.e., \verb{sigma^2_m ~ IG(aa2, bb2)}}
\item{c3, d3}{Shape and scale parameters for the gamma prior on \code{phi}, i.e., \code{phi ~ Gamma(c3, d3)}}
}}

\item{iteration}{Total number of MCMC iterations.}

\item{burnin}{Number of initial iterations to discard as burn-in.}

\item{jump}{Thinning interval: number of iterations to skip between two consecutive stored MCMC samples after burn-in.}
}
\value{
A list containing the following components:
\describe{
\item{MWT}{Posterior samples of the time-varying scale parameter (W).}
\item{MMT}{Posterior samples of the time-varying shape parameter (M).}
\item{MbmT}{Posterior samples of the intercept term for \code{M}.}
\item{MbwT}{Posterior samples of the intercept term for \code{W}.}
\item{MPsi}{Posterior samples of the latent state vectors for the scale component.}
\item{MBeta}{Posterior samples of the latent state vectors for the shape component.}
\item{MPsi0}{Posterior samples of the initial state of \code{Psi}.}
\item{MBeta0}{Posterior samples of the initial state of \code{Beta}.}
\item{Mvw}{Posterior samples of the variance parameter for \code{W}.}
\item{Mvm}{Posterior samples of the variance parameter for \code{M}.}
\item{Mbw}{Posterior samples of regression coefficients for \code{W}.}
\item{Mbm}{Posterior samples of regression coefficients for \code{M}.}
\item{MVpsi}{Posterior samples of the covariance matrix for \code{Psi}.}
\item{MVbta}{Posterior samples of the covariance matrix for \code{Beta}.}
\item{MW}{Posterior mean of the W process (scale).}
\item{MM}{Posterior mean of the M process (shape).}
}
}
\description{
This function performs Bayesian inference for a class of nonhomogeneous Poisson spatiotemporal models. The intensity function follows a Weibull form with time-varying scale and shape parameters modeled through a state-space prior structure. Spatial dependence is incorporated via anisotropic deformation, and model parameters are estimated using a Markov chain Monte Carlo (MCMC) algorithm.
}
